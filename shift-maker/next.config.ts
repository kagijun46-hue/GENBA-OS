import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Allows reading/writing files in the data/ directory from API routes
  serverExternalPackages: [],
};

export default nextConfig;
